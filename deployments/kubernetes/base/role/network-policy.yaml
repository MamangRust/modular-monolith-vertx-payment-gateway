apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: role-netpol
  namespace: payment-gateway
spec:
  podSelector:
    matchLabels:
      app: role
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ecommerce

  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ecommerce

