apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: transaction-netpol
  namespace: payment-gateway
spec:
  podSelector:
    matchLabels:
      app: transaction
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ecommerce

  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ecommerce

